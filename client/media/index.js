/**
 * Media Library styling. There is no interactive React app here — every
 * action is a plain server-rendered link (see Admin/MediaLibraryIntegration)
 * so the Backbone media views are never touched — this entry exists only to
 * ship the small stylesheet those links use.
 */
import '../shared/styles.scss';
